<div class="card border-light shadow-sm">
    <div class="card-header">
        <div class="row align-items-center">
            <div class="col">
                <h2 class="h5">{{ 'tutorials'|trans|capitalize }}</h2></div>
            <div class="col text-right">
            {% if editPath is  defined %}
                <a href="{{ path(editPath, {'id':'new' }) }}" class="btn btn-primary">{{ 'add'|trans|capitalize }}</a>
            {% else %}
                <a href="{{ path('app_tutorial_index') }}" class="btn btn-sm btn-secondary">See all</a>
            {% endif %}
            </div>
        </div>
    </div>
    <div class="table-responsive">
        {% if tutorials is  defined and tutorials|length > 0 %}
        <table class="table align-items-center table-flush">
            <thead class="thead-light">
            <tr>
                <th scope="col">{{ 'position'|trans|capitalize }}</th>
                <th scope="col">{{ 'organization'|trans|capitalize }}</th>
                <th scope="col">{{ 'status'|trans|capitalize }}</th>
                <th scope="col">Bounce rate</th>
            </tr>
            </thead>
            <tbody>
            {% for tutorial in tutorials|slice(0, 10) %}
                <tr>
                    <th>{{ tutorial.name }}</th>
                    <td> </td>
                    <td> </td>
                    <td>
                        {% if editPath is  defined %}
                            <a href="{{ path(editPath, {'id':tutorial.id }) }}" class="btn btn-primary">{{ 'edit'|trans|capitalize }}</a>
                        {% else %}
                            <button type="button" class="btn btn-primary" data-toggle="modal" data-target="#modal-{{ tutorial.id }}">{{ 'edit'|trans|capitalize }}</button>
                        {% endif %}
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
    {% else %}
    <div class="card-body">
        {{ 'no tutorials found'|trans|capitalize }}
    </div>
    {% endif %}
</div>
