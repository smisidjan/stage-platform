
<section class="section section-lg bg-soft">
    <div class="container">
        <div class="row justify-content-between align-items-center">
            <div class="col-lg-5">
                <h2 class="h1">{{ 'challenges'|trans|capitalize }}</h2>
                <p class="mb-4 lead">{{ 'think about problems instead of positions'|trans|capitalize }}</p>
                <p class="mb-4">{{ 'a traditional interhsip is a position in a compony, but that focus on what company\'s are used to. Instead we should focus on what internsneed to learn and that is know day\'s prodemintly problem solivng skill. So instead of offering a student an interhsip profide them with an challange to solve. Either alon or in teams '|trans|capitalize }}</p>
                <div class="d-flex justify-content-between mt-lg-4 mb-5">
                    <div>
                        <a href="{{ path('app_challenge_index') }}" class="btn btn-primary animate-up-2"><span class="fas fa-bullseye-arrow mr-2"></span> {{ 'challenges'|trans|capitalize }}</a>
                    </div>
                </div>
            </div>
            <div class="col-lg-6">
                <img src="./bundles/rocket/assets/img/presentation-mockup-dashboard.png" alt="Dashboard Preview">
            </div>
        </div>
        <div class="row justify-content-center my-5 mt-lg-6">
            <div class="col-6 col-md-3 text-center mb-4">
                <div class="icon icon-shape icon-lg bg-white shadow-lg border-light rounded-circle icon-primary mb-4">
                    <span class="fas fa-pager"></span>
                </div>
                <h3 class="font-weight-bolder">{{ 'teams'|trans|capitalize }}</h3>
                <p class="text-gray">{{ 'work together online'|trans|capitalize }}</p>
            </div>
            <div class="col-6 col-md-3 text-center mb-4">
                <div class="icon icon-shape icon-lg bg-white shadow-lg border-light rounded-circle icon-primary mb-4">
                    <span class="fas fa-puzzle-piece"></span>
                </div>
                <h3 class="font-weight-bolder">{{ 'pitches'|trans|capitalize }}</h3>
                <p class="text-gray">{{ 'get solutions'|trans|capitalize }}</p>
            </div>
            <div class="col-6 col-md-3 text-center">
                <div class="icon icon-shape icon-lg bg-white shadow-lg border-light rounded-circle icon-primary mb-4">
                    <span class="fab fa-sass"></span>
                </div>
                <h3 class="font-weight-bolder">{{ 'products'|trans|capitalize }}</h3>
                <p class="text-gray">{{ 'for student portfolios'|trans|capitalize }}</p>
            </div>
            <div class="col-6 col-md-3 text-center">
                <div class="icon icon-shape icon-lg bg-white shadow-lg border-light rounded-circle icon-primary mb-4">
                    <span class="fas fa-book"></span>
                </div>
                <h3 class="font-weight-bolder">{{ 'bountys'|trans|capitalize }}</h3>
                <p class="text-gray">{{ 'prices for solutions'|trans|capitalize }}</p>
            </div>
        </div>
    </div>
</section>
