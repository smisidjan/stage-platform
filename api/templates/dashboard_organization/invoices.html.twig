{% extends "dashboard/base.html.twig" %}

{% block content %}
    <!-- breadcrumbs -->
    <div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center py-4">
        <div class="d-block mb-4 mb-md-0">
            <nav aria-label="breadcrumb" class="d-none d-md-inline-block">
                <ol class="breadcrumb breadcrumb-dark breadcrumb-transparent">
                    <li class="breadcrumb-item"><a href="{{ path('app_default_index') }}"><span class="fas fa-home"></span></a></li>
                    <li class="breadcrumb-item"><a href="{{ path('app_dashboard_index') }}">{{ 'Dashboard'|trans|capitalize }}</a></li>
                    <li class="breadcrumb-item active">{{ 'invoices'|trans|capitalize }}</li>
                </ol>
            </nav>
            <h2 class="h4">{{ 'invoices'|trans|capitalize }}</h2>
            <p class="mb-0">{{ 'here you can see and pay all your invoices'|trans|capitalize}}</p>
        </div>
    </div>

    <div class="row">
        <div class="col-12 col-xl-8">
            <div class="row">
                <div class="col-12 mb-4">
                    {% for invoice in invoices %}
                        {{ invoice.name }}
{#                    {% include 'dashboard/tables/internships.html.twig' with {'editPath': 'app_dashboardorganization_internship'} %}#}
                    {% endfor %}
                </div>
            </div>
        </div>
        <div class="col-12 col-xl-4 mb-4">
            <div class="col-12 px-0 mb-4">
                <div class="card border-light shadow-sm">
                    <div class="card-body">
                        <h2 class="h5">{{ 'invoices'|trans|capitalize }}</h2>
                        <p>{{ 'text about invoices'|trans|capitalize }}</p>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
